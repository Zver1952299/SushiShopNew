import React from 'react';
import CartBlock from '../components/CartBlock';

const Cart: React.FC = () => {
  return (
    <div className="wrapper">
      <CartBlock />
    </div>
  );
};

export default Cart;
